server:
  grpc_addr: ":50051"
  webhook_addr: ":8080"

database:
  path: "stargate.db"

sms:
  gate_url: "http://192.168.1.1"        # SMS Gate app / RUT241 API base URL
  api_key: "your-api-key-here"          # Bearer token for outbound API calls
  webhook_secret: "your-signing-key"    # SMS Gate → Settings → Webhooks → Signing Key