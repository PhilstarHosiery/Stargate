# bin/

Local tool binaries. Not committed to the repository (gitignored).

| File | Version | Source |
|---|---|---|
| `protoc.exe` (Windows) | 34.0 | https://github.com/protocolbuffers/protobuf/releases |
| `protoc` (FreeBSD/Linux) | 34.0 | https://github.com/protocolbuffers/protobuf/releases |

Download the appropriate build for your platform and place it here before running `gen.bat` (Windows) or `make gen` (FreeBSD).
